Q1. Write a Python program to store your:
 Name
 Age
 City
Then print al values.
Q2. Write a program to check whether a number entered by the user is positive, negative, or
zero.
Q3. Write a program to check whether a number is even or odd.
Q4. Write a program to take student's marks as input and print:- "Pass" if marks are greater than or equal to 50- "Fail" otherwise
Q5. Write a program to check whether a person is eligible to vote or not.
Condition:- Age 18 or above → Eligible- Otherwise → Not Eligible
Q6. Write a program to input temperature and print:- `"Hot"` if temperature is greater than 35- `"Normal"` if temperature is between 20 and 35- `"Cold"` otherwise
Q7:
Write a Python program for a Student Information System that performs the folowing tasks:
Requirements:
1. Take input from the user for a student with the folowing details:- Name (string)- Age (integer)- Marks (float)- Is the student enroled? (boolean: True/False)
2. Use **logical operators (`and`, `or`) to check conditions.
3. Apply the folowing conditions:
Condition 1 (Pass/Fail using AND):
If marks are **greater than or equal to 50 AND age is greater than or equal to 18**, print:
Student is PASS and eligible for higher studies
Condition 2 (Special Case using OR):
If marks are **greater than or equal to 80 OR enroled is True**, print:
Student is Excelent / Active Participant
Condition 3 (Failure Case):
Otherwise print:
Student needs improvement
4. Finaly, print al the student details in a **proper formatted output** like this:----- Student Record----
Name: Ali
Age: 20
Marks: 75
Enroled: True
Result: PASS / FAIL / EXCELLENT (based on condition)
